
# Core dependencies
openreview-py>=1.55.0
pdf2image>=1.17.0
requests>=2.32.0
tqdm>=4.67.0

# PyTorch / OCR stack
torch==2.6.0
torchvision==0.21.0

# Transformers & AI
transformers==4.46.3
tokenizers==0.20.3
accelerate==1.13.0
einops>=0.8.0
safetensors>=0.7.0

# Utilities
addict>=2.4.0
easydict>=1.13
Pillow>=11.0.0
numpy>=2.0.0
matplotlib>=3.10.0  # Required for some visualizations and unexpected dependencies in OCR
openai>=2.15.0     # For Online Analysis
anthropic>=0.39.0  # For Claude online analysis

# DeepSeek OCR official docs also require:
# flash-attn==2.7.3 --no-build-isolation
# Install it manually in a CUDA development environment with nvcc available.
